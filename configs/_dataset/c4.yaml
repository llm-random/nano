defaults:
  - default
  - _self_

trainer:
  train_dataloader:
    dataset_path: ${cluster_switch.train_path_c4}
    dataset_split: train
    dataset_type: c4
    num_workers: ${cluster_switch.num_workers}

  eval_dataloader:
    dataset_path: ${cluster_switch.eval_path_c4}
    dataset_split: validation
    dataset_type: c4
    num_workers: ${cluster_switch.num_workers}
