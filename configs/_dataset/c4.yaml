defaults:
  - default
  - _self_

trainer:
  train_dataloader:
    dataset:
      data_generator:
        path: ${cluster_switch.train_path_c4}
  eval_dataloader:
    dataset:
      data_generator:
        path: ${cluster_switch.eval_path_c4}
