defaults:
  - base_scheduler_comparison

trainer:
  scheduler:
    _partial_: true
    _target_: src.plasticity.schedulers.WSDScheduler
    n_steps: ${trainer.n_steps}
    warmup_fraction: 0.01
    decay_fraction: 0.1
    final_lr_fraction: 0.1

infrastructure:
  metric_logger:
    name: Plasticity_Standard_WSD
