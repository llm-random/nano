trainer:
  checkpoint:
    save:
      type: nano
      # interval: ${eval:'((${trainer.n_steps} - 1) / 4)'}
      interval: -1
      steps: [1, 10_001, 20_001, 40_001, 60_001, 80_001, 100_001, 120_001, 140_001]
      path: /lustre/pd03/plgrid/plgllmefficont3/context_scaling/models/icml_workshop
      model_checkpoint_filename: __model_checkpoint_filename.pt
      training_state_filename: __training_state_filename.pt

    load:
      type: nano
      path: null
      model_checkpoint_filename: __model_checkpoint_filename.pt
      training_state_filename: __training_state_filename.pt # not tested
      only_weights: true # false not tested
