defaults:
  - _cluster@_here_: lem
  - _infrastructure@_here_: default
  - _self_

hydra:
  job:
    chdir: false

infrastructure:
  slurm:
    gres: gpu:hopper:1
    job-name: ctx_eval
    time: "00:30:00"

eval:
  tags: [2503_grid]
  negative_tags: [remove]
  out_dir: 2503_grid
  # dataset_dir: /storage_nvme_1/llm-random/datasets/c4/long_context_2048n8192
  dataset_dir: /lustre/pd01/plgrid/plgllmefficont2/datasets/c4/long_context_2048n8192
  seq_len: 2048
  batch_size: 16
  model_step: null  # null → trainer.n_steps - 1 per run; or list e.g. [200000, 320000]
  name: ${eval.out_dir}
